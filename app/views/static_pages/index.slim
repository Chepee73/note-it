doctype html
html
	body
		h1 Welcome to Note-It!
		div class="form"
			=form_tag   do
				=label_tag(:user, "User")
				=text_field_tag(:user)
			end
		div class="form"
			=form_tag   do
				=label_tag(:password,"Password")
				=text_field_tag(:password)
			end
			=submit_tag("Login")
